# MeowView 修复记录

日期：2026-04-15
文件：`src/views/MeowView.vue`

## 修复目标
- 修复页面中文乱码和损坏标签。
- 保持猫叫统计、饼图和事件列表的既有数据流不变。
- 确认页面仍可正常构建。

## 本次改动
- 重写了页面模板中的所有乱码文案，恢复为正常中文。
- 修复了多个损坏的 HTML 标签，避免模板解析异常。
- 保留原有 3 个统计卡片、柱状图、饼图、事件筛选和事件表格结构。
- 统一了图表标题、筛选项、空状态和表格列名文案。
- 保留 `getMeowStats()` 和 `getMeowEvents(hours)` 的接口调用方式。
- 保留 ECharts SVG 渲染方式和初始化、销毁、resize 逻辑。
- 增加了请求失败时的回退值，避免页面因为接口异常直接显示脏状态。

## 验证结果
- 执行 `npm run build` 成功。
- `MeowView` 当前可以正常编译进入生产构建。

## 说明
- 这次只处理 `MeowView`，没有顺手修改其他仍存在乱码的页面。
- 如果后续继续修 UI，建议再统一处理 `DashboardView.vue`、`SensorView.vue`、`router/index.js` 等文件中的乱码文案。

